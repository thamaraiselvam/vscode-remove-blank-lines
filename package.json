{
	"name": "remove-blank-lines",
	"displayName": "remove-blank-lines",
	"description": "this extension remove all blank lines in a file",
	"version": "0.0.1",
	"engines": {
		"vscode": "^1.35.0"
	},
	"categories": [
		"Other"
	],
	"activationEvents": [
		"onCommand:extension.removeOnSelectedLines",
		"onCommand:extension.removeOnDocument"
	],
	"main": "./out/extension.js",
	"contributes": {
		"commands": [
			{
				"command": "extension.removeOnSelectedLines",
				"title": "In Selection",
				"category": "Remove Blank lines"
			},
			{
				"command": "extension.removeOnDocument",
				"title": "In Document",
				"category": "Remove Blank lines"
			}
		],
		"keybindings":[
			{
				"command": "extension.removeOnSelectedLines",
				"key": "Ctrl+Shift+A"
			},
			{
				"command": "extension.removeOnDocument",
				"key": "Ctrl+Shift+Z"
			}
		]
	},
	"scripts": {
		"vscode:prepublish": "npm run compile",
		"compile": "tsc -p ./",
		"watch": "tsc -watch -p ./",
		"postinstall": "node ./node_modules/vscode/bin/install",
		"test": "npm run compile && node ./node_modules/vscode/bin/test"
	},
	"devDependencies": {
		"typescript": "^3.3.1",
		"vscode": "^1.1.28",
		"tslint": "^5.12.1",
		"@types/node": "^10.12.21",
		"@types/mocha": "^2.2.42"
	}
}
